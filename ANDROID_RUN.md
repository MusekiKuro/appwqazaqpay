# 🚀 Инструкция по запуску Android приложения в VS Code/Cursor

## Предварительные требования

1. **Android SDK** должен быть установлен (обычно в `C:\Users\<ваше_имя>\AppData\Local\Android\Sdk`)
2. **ADB** должен быть в PATH или доступен через Android SDK
3. **Эмулятор Android** должен быть запущен или подключено физическое устройство

## Способы запуска

### Способ 1: Через скрипт (Рекомендуется) ⭐

#### Windows (PowerShell):
```powershell
.\run-android.ps1
```

#### Linux/Mac:
```bash
chmod +x run-android.sh
./run-android.sh
```

Скрипт автоматически:
- ✅ Проверит наличие подключенного устройства/эмулятора
- ✅ Соберет приложение (assembleDebug)
- ✅ Установит APK на устройство (installDebug)
- ✅ Запустит приложение

### Способ 2: Через VS Code Tasks

1. Нажмите `Ctrl+Shift+P` (или `Cmd+Shift+P` на Mac)
2. Введите `Tasks: Run Task`
3. Выберите одну из задач:
   - **Android: Build Debug APK** - только сборка
   - **Android: Install Debug APK** - сборка + установка
   - **Android: Build, Install & Run** - полный цикл (сборка + установка + запуск)
   - **Android: Start Activity** - только запуск (если уже установлено)
   - **Android: Clean Build** - очистка проекта

### Способ 3: Через терминал вручную

```bash
# 1. Сборка приложения
.\gradlew.bat assembleDebug

# 2. Установка на устройство
.\gradlew.bat installDebug

# 3. Запуск приложения
adb shell am start -n com.example.qazaqpaybank/.MainActivity
```

## Запуск эмулятора

Если эмулятор не запущен:

### Через Android Studio:
1. Откройте Android Studio
2. Tools → Device Manager
3. Запустите нужный эмулятор

### Через командную строку:
```bash
# Список доступных эмуляторов
emulator -list-avds

# Запуск эмулятора
emulator -avd <имя_эмулятора>
```

## Проверка подключенных устройств

```bash
adb devices
```

Должен показать список устройств. Если список пуст - запустите эмулятор или подключите устройство.

## Просмотр логов

```bash
# Все логи
adb logcat

# Только логи вашего приложения
adb logcat | grep qazaqpaybank

# Очистка логов
adb logcat -c
```

## Полезные команды ADB

```bash
# Переустановка приложения
adb uninstall com.example.qazaqpaybank
adb install app/build/outputs/apk/debug/app-debug.apk

# Очистка данных приложения
adb shell pm clear com.example.qazaqpaybank

# Остановка приложения
adb shell am force-stop com.example.qazaqpaybank

# Скриншот
adb shell screencap -p /sdcard/screenshot.png
adb pull /sdcard/screenshot.png
```

## Решение проблем

### Проблема: "adb: command not found"
**Решение:** Добавьте Android SDK platform-tools в PATH:
- Windows: `C:\Users\<ваше_имя>\AppData\Local\Android\Sdk\platform-tools`
- Linux/Mac: `~/Android/Sdk/platform-tools`

### Проблема: "No devices found"
**Решение:** 
1. Убедитесь, что эмулятор запущен
2. Проверьте: `adb devices`
3. Перезапустите ADB: `adb kill-server && adb start-server`

### Проблема: "Gradle build failed"
**Решение:**
1. Проверьте, что Android SDK установлен
2. Проверьте `local.properties` - должен содержать путь к SDK
3. Попробуйте: `.\gradlew.bat clean` затем `.\gradlew.bat assembleDebug`

## Горячие клавиши VS Code

- `Ctrl+Shift+B` - Запуск задачи сборки по умолчанию
- `Ctrl+Shift+P` → `Tasks: Run Task` - Выбор задачи

## Рекомендации

1. **Используйте скрипт** `run-android.ps1` для быстрого запуска
2. **Проверяйте эмулятор** перед запуском (`adb devices`)
3. **Следите за логами** при разработке (`adb logcat`)
4. **Используйте Clean Build** если возникают странные ошибки

---

💡 **Совет:** Добавьте скрипт `run-android.ps1` в PATH или создайте алиас для еще более быстрого запуска!

